export default function Button(){

    const backgroundColor = 'purple';
    const color = 'white';

    const styles = {
        backgroundColor,
        color
    }
    return(
        <button style={styles}>
            Button
        </button>
    )
}